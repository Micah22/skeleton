const raceService = {
    //
    getRacesByDistrict: (districtId) => {
        //Return an array of races that matc this district ID
        //or are state-wide/county-wide races
    }
}

module.experts = raceService